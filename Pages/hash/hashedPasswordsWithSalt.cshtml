@page
@model CryptographyCore.Pages.hash.hashedPasswordsWithSaltModel
@{
    ViewData["Title"] = "hashedPasswordsWithSalt";
}

Value to compute hash of, <b>@Model.data</b><p />
Salt, <b>@Model.salt</b><p />
Default hash, <b>@Model.hash</b><p />
Hash with salt, <b>@Model.hashWithSalt</b><p />
Hash with salt and random data, <b>@Model.hashWithSaltAndRandom</b><p />
